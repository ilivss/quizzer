{
  "scripts": {
    "dev": "sudo docker compose up -d && concurrently \"cd ./api && yarn run dev\" \"cd ./quizmaster-client && yarn run start\""
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
